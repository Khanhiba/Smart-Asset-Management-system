services:
  - type: web
    name: nexus-assets
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev --prefix server && npm ci --include=dev --prefix client && npm run build --prefix client
    startCommand: npm start
    initialDeployHook: npm run seed --prefix server
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: SEED_DEMO_DATA
        value: true
      - key: JWT_SECRET
        generateValue: true
      - key: MONGODB_URI
        sync: false
